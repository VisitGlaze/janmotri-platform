const config = {
    API_BASE_URL: import.meta.env.VITE_API_BASE_URL,

    APP_NAME: "Janmotri Oil",

    DEFAULT_LANGUAGE: "en",

    SUPPORTED_LANGUAGES: ["en", "hi", "gu"],

    TOKEN_KEY: "janmotri_admin_token",
};

export default config;